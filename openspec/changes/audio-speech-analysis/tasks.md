## 1. 持久化契约与配置迁移

- [x] 1.1 为 `SpeechQuality`、转写文档、素材和运行信息新增可选字段、`0.4` schema 写入，以及新增字段默认为 null 的 `0.3` schema 读取补充失败测试。
- [x] 1.2 实现 `SpeechQuality`、人声片段和转写模型、`Asset.speech_quality`、`AnalysisInfo.audio_analysis_model` 及 schema version 兼容行为。
- [x] 1.3 为必填的 `audio_analysis_model`、移除 `transcription_model`、旧字段迁移错误和脱敏模型配置摘要补充失败测试。
- [x] 1.4 修改软件配置字段及所有运行时和示例引用，要求 `sample` 和 `full` 必须配置该字段，并更新敏感字段处理。
- [x] 1.5 新增音频缓存和素材级转写路径辅助函数，确保项目目录初始化会创建所需父目录。

## 2. 音频提取与分块缓存

- [x] 2.1 增加单元和组件测试素材，并为无音轨输入及短于、等于、长于 300 秒边界的 WAV 提取补充失败测试。
- [x] 2.2 使用 `ffmpeg` 实现 `AudioChunk` 和 `AudioExtractor`，生成 16 kHz 单声道 16-bit PCM WAV 分块，携带原视频偏移并完整无重叠地覆盖时间轴。
- [x] 2.3 为源文件指纹缓存校验、非 force 复用、损坏或过期缓存拒绝，以及 force 重新提取补充失败测试。
- [x] 2.4 在 `cache/audio/<asset_id>/` 下实现提取缓存元数据、校验、复用和 force 替换。
- [x] 2.5 为近数字静音、明显非静音 PCM，以及 Analyzer 静音块跳过 Provider 并保存 `none` 补充失败测试。
- [x] 2.6 实现本地 PCM 峰值/RMS 静音门，把确定静音块作为成功的 `none` 结果参与聚合，非静音块继续调用 Provider。

## 3. 响应解析与素材聚合

- [x] 3.1 为顶层错误、枚举校验、局部时间范围、非法片段隔离、偏移恢复、排序、重叠合并和文本拼接补充 Parser 失败测试。
- [x] 3.2 实现 `AudioAnalysisParser`，为单个片段记录 warning，并在无法保留可信分类时把整块判为失败。
- [x] 3.3 为 `none` 与合法片段矛盾、`clear` 但文本为空、全部成功聚合，以及所有部分失败和全部失败组合补充失败测试。
- [x] 3.4 实现矛盾结果规范化和保守的素材级聚合，包括明确的分析不完整 warning。
- [x] 3.5 为纯标点、完整时间码、正常中英文、纯数字、笑声文本、拟声词和无效/有效兄弟片段组合补充文本格式失败测试。
- [x] 3.6 在 Parser 中实现最小文本格式校验，只丢弃纯标点和完整时间码并记录 warning，不过滤纯数字、笑声文本或拟声词。

## 4. Gemini 音频 Provider

- [x] 4.1 为配置模型、`/chat/completions`、base64 WAV `input_audio`、只返回 JSON、人类口语范围、原语言转写，以及禁止翻译和摘要补充 Provider 合约测试。
- [x] 4.2 实现独立的 `AudioAnalysisProvider` 及 prompt/响应边界，不复用视觉 prompt 或视觉结果模型。
- [x] 4.3 为分块级瞬时错误重试和不支持 `reasoning_effort` 时去掉参数的一次性回退补充失败测试。
- [x] 4.4 实现低 reasoning 请求、不支持参数时的回退、现有重试分类，以及不记录凭证、音频编码、完整 prompt 和完整原始响应的日志策略。

## 5. 转写原子存储

- [x] 5.1 为完整文档校验、临时文件清理、原子发布、素材相对路径引用，以及校验或写入失败后保留旧结果补充 `TranscriptStore` 失败测试。
- [x] 5.2 通过已校验的临时文件和原子替换实现转写 JSON 写入，只有发布成功后才更新素材字段。
- [x] 5.3 增加并通过测试：首次部分成功且检测到人声时允许保存并记录不完整 warning，但部分或失败的 force 重跑不得替换已有完整有效转写。

## 6. Analyzer 与 CLI 编排

- [x] 6.1 增加 Analyzer 测试，证明无音轨视频跳过提取和 Provider 调用、独立音频素材保持原行为、已选视频默认执行音频分析。
- [x] 6.2 把提取、逐块 Provider 调用、解析、聚合、转写存储和音频模型运行信息集成到 `sample` 和 `full`。
- [x] 6.3 为 `has_audio=true`、`speech_quality=null` 且画面已分析的视频补充增量失败测试，包括非 force 时不重复调用视觉 Provider。
- [x] 6.4 更新处理资格和检查点逻辑，使音频子步骤能在崩溃后恢复；`--force` 同时重跑两条路径，并保护已有有效转写。
- [x] 6.5 增加失败隔离测试并实现素材和项目失败、结构化 warning，确保不删除画面字段，也不改变成功的画面 `analysis_status`。
- [x] 6.6 扩展 CLI 和启动测试，验证音频配置缺失或使用旧字段时会在模型请求前失败，并验证成功运行会报告实际音频分析模型。

## 7. CSV、HTML 与 Eagle 消费

- [x] 7.1 增加导出失败测试，要求 `assets.csv` 和每个相关 `review.html` 素材条目都包含 `speech_quality`。
- [x] 7.2 实现 CSV 人声质量导出，以及 review 中的分类、原视频时间范围和原语言转写展示。
- [x] 7.3 增加并通过 review 测试，覆盖人声质量为 null、转写引用缺失、不可读或内容非法，并确保单个素材降级不阻断其他素材。
- [x] 7.4 为 Eagle 人声质量标签、带原视频时间范围的描述章节、无人声文本、null 跳过和转写损坏降级补充失败测试。
- [x] 7.5 扩展 Eagle 薄映射层，加载并校验素材级转写，写入稳定质量标签和“语音识别”描述章节，且不把转写文本写入标签。

## 8. 端到端验证

- [x] 8.1 运行模型、配置、提取器、Parser、Provider、Store、Analyzer、CLI 和导出相关测试，并解决回归。
- [ ] 8.2 运行完整自动化测试套件，并验证日志及生成的 `cut_index.json`、转写 JSON、CSV 和 HTML 不包含敏感音频或请求载荷。
- [ ] 8.3 使用真实视频覆盖无音轨、无人声、清晰人声、不清晰人声、中文、英文、中英混说、跨五分钟边界和时长超过五分钟的场景；结合播放人工核对分类、文本可用性和原视频时间对齐。
